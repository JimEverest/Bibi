# Bibi 离线语音输入部署手册
## （本机已验证版 → 内网复现版）

> 验证环境：Windows 11 / Python 3.13 / 纯 CPU（无 NVIDIA GPU）
> 验证结果：中英文混合识别成功，13 秒音频推理约 1 秒，模型加载 5.2 秒
> **2026-09-01 更新：默认识别引擎已切换为 SenseVoice（中英混说更准），新增术语词典与 F2 防重复修复**

---

## 〇、准确率升级说明（方案 1+2+3 已实施）

| 改动 | 内容 | 效果 |
|---|---|---|
| 方案1：SenseVoice 引擎 | 设环境变量 `ASR_ENGINE=sensevoice`（启动脚本已默认设置），模型在 `models/iic/SenseVoiceSmall/` | 英文/中英混说显著提升，英文长句可完整识别（实测模型上下文协议/MCP 主题长句几乎全对）；加载 2.6s、推理速度更快；自动剥离情绪标签 |
| 方案2：术语词典 | 项目根目录 `terms.txt`，格式"错误写法→正确写法"（Tab 或逗号分隔），改完 5 秒自动生效 | 已内置 MCP/Copilot/Jira/SharePoint/按住Ctrl 等实测错误映射，可持续补充 |
| 方案3：F2 防重复 | 修复按住 F2 触发 Windows 按键自动重复产生 0.1 秒垃圾录音的问题 | 只响应物理按下，测试体验干净 |
| 方案4：单实例锁 | 端口锁（127.0.0.1:57612），第二个实例启动即退出 | 防止多实例并发把同一句话注入两遍（字符交错乱码，如 `tthhee mmeeaassuurr`）。判断是否有实例在跑：`netstat -ano | findstr 57612` |
| 方案5：LLM 二次润色 | 可选 LLM 后处理：去口癖（呃/嗯/um/uh）、合并重复（"我我我"→"我"）、整合自我修正（"周三不对是周四"→"周四"）、纠同音错字。托盘菜单或设置界面开关 | 已实测 DeepSeek 双协议全通过（openai chat 1.7~7s / anthropic messages 1.7~4.8s）；支持 OpenAI（chat/responses 双端点）与 Anthropic 双协议；失败自动降级不阻塞。配置见 config `llm` 段 |
| UI：托盘+浮窗+设置界面 | 系统托盘图标（状态变色）+右键菜单+「设置…」对话框；录音时右下角浮窗显示计时 | `--no-tray` 可回纯 CLI 模式 |

### 设置界面（托盘右键 →「设置…」）

- **常规**页：听写快捷键（默认 f2，可改 ctrl+alt+v 等，重启生效）、录音指示浮窗开关、文本输出方式（auto/type/clipboard/unicode）、单次录音上限（秒）、麦克风输入增益、**保存录音 WAV 开关**（`audio.save_recordings`，**默认关闭**——识别临时文件即用即删，不在 logs/ 留档；勾选后每次转录会在 logs/ 写 recent.wav 供调试，磁盘会持续增长，重启生效）、**一键清理日志与录音文件…** 按钮（统计 wav+日志体积→确认框→删除；当天活跃日志被占用时自动改为清空内容）
- **LLM 润色**页：启用开关、协议 schema（openai/anthropic）、端点风格（openai 协议下为 chat/responses；anthropic 协议使用 Messages API，无需选择自动置灰）、Endpoint URL、API Key（掩码显示）、模型名、temperature、超时、**思考模式开关**、**领域词汇**、**润色 Prompt 编辑框**、**网络设置**（HTTP/HTTPS 代理 + 跳过 SSL 证书校验，内网经代理访问外部 LLM 时配置）；底部有「测试 LLM 连接」按钮，保存前可直接验证连通性（测试句内置同音错字场景）
- 保存位置：项目根目录 `vocotype_config.json`（程序启动时自动加载，无需 `--config` 参数）

### LLM 润色配置示例（config json 或 vocotype_config.json）
```json
{
  "llm": {
    "enabled": true,
    "schema": "openai",            // "openai" 或 "anthropic"
    "endpoint": "https://内网LLM地址/v1/chat/completions",
    "style": "chat",               // openai 可选 "chat" 或 "responses"
    "api_key": "sk-xxx",
    "model": "qwen2.5-7b-instruct",
    "temperature": 0.3,
    "timeout_seconds": 15,
    "disable_thinking": true,      // 关闭模型思考模式（强烈建议，见下）
    "vocabulary": "线程, 队列, …",  // 领域词汇提示，帮助 LLM 纠同音错字
    "prompt": ""                   // 留空用内置最小化润色模板
  }
}
```

**⚠️ 思考模式（thinking）必读**：DeepSeek V4 等新模型**默认开启 thinking 且力度为 high**，润色这种小任务也会思考十几秒甚至超时（实测 30 秒超时）。程序默认发送关闭参数：两个端点统一用 `"thinking": {"type": "disabled"}`（官方文档里 Anthropic 格式的 `reasoning.effort=none` 实测被网关拒绝 502，勿用）。关闭后实测延迟 **0.8~1.0 秒**。若确需深度纠错可设 `disable_thinking: false`（openai 端点可用 `reasoning_effort` 控制力度：low/high/max）。
内网可接 Ollama（`http://127.0.0.1:11434/v1/chat/completions`）或 vLLM 等任何 OpenAI 兼容服务。
注意：启用润色后，识别文本会发送到所配 endpoint；纯离线合规场景请保持 `enabled: false`。
**DeepSeek 云端实测（2026-09-02）**：`schema=openai` + `https://api.deepseek.com/v1/chat/completions` 与 `schema=anthropic` + `https://api.deepseek.com/anthropic/v1/messages` 双协议均通过，model=`deepseek-v4-flash`。**内网无外网时云端 API 不可达，润色会静默降级为原文本（不报错不阻塞）**；内网要用润色请接 Ollama/vLLM 本地服务。

**引擎回退**：如需切回旧 Paraformer 引擎（支持热词），把启动脚本中 `set ASR_ENGINE=sensevoice` 注释掉即可。
**注意**：SenseVoice 输出无标点（中文文本会自动过标点模型补标点，纯英文无标点属正常）；英文缩写会输出为逐字母（"m c p"），词典里可加一条映射 `m c p→MCP`。

---

## 一、本机测试（现在就能试）

语音输入主程序**当前正在后台运行**（F2 热键已注册）。

### 立即测试步骤
1. 打开任意可输入文字的窗口（记事本、微信、Word 等），点好光标
2. 按 **F2** 开始录音（说一段中英文混合的话，例如："我们明天上午九点开一个 meeting 讨论一下 budget"）
3. 再按 **F2** 结束录音
4. 约 1~2 秒后，识别文本自动输入到光标位置
5. 测完在终端按 Ctrl+C 或结束进程退出

> 注意：全局热键需要管理员权限。如果 F2 无反应，用管理员身份重新启动终端再运行主程序。
> 录音文件调试：每次录音会保存在 `vocotype-cli/logs/recent.wav`，可以回放确认麦克风采集是否正常。

### 手动启动方式（下次自己启动）
```bat
cd C:\Users\tianw\WorkBuddy\2026-09-01-14-57-35\vocotype-cli
C:\Users\tianw\.workbuddy\binaries\python\envs\vocotype\Scripts\python.exe main.py
```
- F2 = 开始/停止录音；退出 = Ctrl+C
- 改热键：编辑 `app/config.py` 或传 `--config 配置.json`（hotkeys.toggle 字段）

---

## 二、本机已验证的性能数据

| 指标 | 首次运行 | 稳态运行 |
|---|---|---|
| 模型加载 | 5.7 秒 | 5.2 秒 |
| librosa 预热 | 114.9 秒（numba JIT 编译，一次性） | 2.9 秒 |
| 13 秒音频推理 | ≈1 秒 | ≈1 秒 |
| 中英混说识别 | 准确（meeting/budget/KPI/review/report 全部正确） | 同左 |
| 标点恢复 | 自动加标点，效果好 | 同左 |
| 内存占用 | — | 官方标称约 700MB |

已知小瑕疵：同音英文词可能识别偏差（实测 "team" → "tim"）。可通过热词机制缓解（Paraformer 支持 hotword 参数）。

### 已修复：按 F2 录音被切碎 / 识别出"嗯"（核心问题）
**现象**：按 F2 后识别结果全是"嗯。"或空白，还伴随 `input wav is silence or noise` 和 `list index out of range` 报错。

**原因**：Windows 按住按键会连续发送 key-down 事件（自动重复）。程序默认在"按下"时触发热键，导致一次按 F2 被 理解成"开始→停止→开始→停止"十几次切换，每次只录到 0.1 秒碎片，真正说的话根本没录进去。日志特征：`总样本数=1920`（0.12 秒）。

**修复**（已应用）：`app/hotkeys.py` 改为 `trigger_on_release=True`（松开按键才触发），一次按键只切换一次。**正确用法：按一下 F2 松开 → 看到开始录音 → 说话 → 再按一下 F2 → 1~2 秒后出字**。日志中 `总样本数` 应为说话时长的 16000 倍（如说 5 秒 ≈ 80000）。

### 已修复：静音音频导致的报错
`app/funasr_server.py` 对静音/噪音输入返回空列表时未判空导致 IndexError 崩溃报错，已加判空（不影响功能，只消除报错噪音）。

### 已修复：麦克风音量过低问题（重要）
**现象**：按 F2 录音后识别报 `list index out of range`，或输出无意义的 "the the the..."。日志里 `recent.wav` 电平极低（peak < 0.02）。

**原因**：部分机器内置麦克风录音信号很弱（实测 peak 仅 0.009，正常应 > 0.1），Paraformer 拿到近乎静音的音频会返回空结果。

**修复**（已应用到代码）：
- `app/config.py` 新增 `audio.gain` 配置（默认 `12.0`），录制音频统一放大 12 倍并做削波保护
- 如果增益后仍识别不准，调大 `gain`（如 `20.0`）；如果出现爆音/破音则调小（如 `6.0`）
- 同时建议在 Windows 声音设置里把麦克风输入音量调到 80~100%，关闭"音频增强"

---

## 三、内网复现清单（琪琪机器上照做）

### 第 0 步：先搞清用哪个模型（2026-09-01 更新）

代码用哪个模型由环境变量 `ASR_ENGINE` 决定（启动脚本已默认 `sensevoice`）：

| 引擎 | 模型（modelscope id） | 缓存目录名 | 大小 | 是否必需 |
|---|---|---|---|---|
| `sensevoice`（**默认**） | `iic/SenseVoiceSmall` | `...\iic\SenseVoiceSmall\` | 243MB（量化） | **必需** |
| `paraformer`（回退） | `iic/speech_paraformer-large_asr_nat-zh-cn-16k-common-vocab8404-onnx` | 同名目录 | 228MB | 可选 |
| 附带：VAD | `iic/speech_fsmn_vad_zh-cn-16k-common-onnx` | 同名目录 | 534KB | 可选（默认不加载） |
| 附带：标点（仅 paraformer 用） | `iic/punc_ct-transformer_zh-cn-common-vocab272727-onnx` | 同名目录 | 274MB | 可选 |

> 注意别被名字迷惑：**Paraformer-“large”是旧引擎**（中文为主，"large"是架构代号）；新默认引擎的模型反而叫 **SenseVoice-“Small”**（中英混说更准）。两者是不同模型，不是同一个。

**最省方案**：只拷 `models\iic\SenseVoiceSmall\`（243MB）即可用默认引擎跑起来。
**完整方案**：直接拷整个 `vocotype_offline_bundle\`（1.1GB），两套模型都带，随时可回退。

### 第 1 步：外网机器打包以下内容（U盘/内网摆渡）

| # | 内容 | 位置 | 大小 |
|---|---|---|---|
| 1 | 代码仓库（含 venv 可选） | `vocotype-cli\` 整个目录 | ~5MB（不含venv） |
| 2 | **SenseVoice 模型（默认引擎必需）** | `C:\Users\tianw\.cache\modelscope\hub\models\iic\SenseVoiceSmall\` 中的 `model_quant.onnx`、`chn_jpn_yue_eng_ko_spectok.bpe.model`、`am.mvn`、`config.yaml` 四个文件 | 243MB |
| 3 | Paraformer 模型（可选，回退用） | `...\iic\speech_paraformer-large_asr_nat-zh-cn-16k-common-vocab8404-onnx\` | 228MB |
| 4 | VAD 模型（可选） | `...\iic\speech_fsmn_vad_zh-cn-16k-common-onnx\` | 534KB |
| 5 | 标点模型（可选，paraformer 引擎用） | `...\iic\punc_ct-transformer_zh-cn-common-vocab272727-onnx\` | 274MB |
| 6 | Python 依赖 wheel 包（可选但强烈建议） | 见下方打包命令 | ~1GB |

> **推荐做法**：以上内容已经全部打好在 `C:\Users\tianw\WorkBuddy\2026-09-01-14-57-35\vocotype_offline_bundle\`（1.1GB，含一键安装脚本），整个文件夹拷进 U 盘即可，无需手动挑文件。

**打包 pip 依赖（在外网机器执行，下载所有 wheel 到本地目录）：**
```bat
C:\Users\tianw\.workbuddy\binaries\python\envs\vocotype\Scripts\python.exe -m pip download ^
  sounddevice==0.5.2 keyboard==0.13.5 pyperclip==1.11.0 soundfile==0.13.1 ^
  jieba==0.42.1 "websockets>=12.0" "numpy<2.2" scipy librosa==0.11.0 ^
  modelscope==1.30.0 funasr_onnx==0.4.1 pyyaml onnxruntime sentencepiece ^
  torch --index-url https://download.pytorch.org/whl/cpu ^
  --extra-index-url https://pypi.org/simple ^
  -d C:\vocotype_wheels
```
把 `C:\vocotype_wheels` 整个目录也拷走。

> kaldi_native_fbank 依赖 funasr_onnx 但 requirements.txt 未列出，wheel 打包命令已包含缺口——如果内网安装时提示缺什么，回外网补 `pip download 缺的包名 -d C:\vocotype_wheels` 即可。

### 第 2 步：内网机器安装

前提：内网机器有 Python 3.10~3.13（64位）。

```bat
:: 1. 还原模型缓存（保持相同目录结构！至少要有 SenseVoiceSmall）
xcopy /E /I "U盘\vocotype_offline_bundle\models\iic" "%USERPROFILE%\.cache\modelscope\hub\models\iic\"

:: 2. 解压/拷贝代码到任意目录，如 D:\vocotype-cli

:: 3. 建虚拟环境
python -m venv D:\vocotype-cli\.venv

:: 4. 离线安装依赖（全部来自本地 wheel，零联网）
D:\vocotype-cli\.venv\Scripts\python.exe -m pip install --no-index --find-links=U盘\vocotype_wheels ^
  sounddevice==0.5.2 keyboard==0.13.5 pyperclip==1.11.0 soundfile==0.13.1 ^
  jieba==0.42.1 "websockets>=12.0" "numpy<2.2" scipy librosa==0.11.0 ^
  modelscope==1.30.0 funasr_onnx==0.4.1 pyyaml onnxruntime sentencepiece torch kaldi-native-fbank

:: 5. 启动（默认 SenseVoice 引擎；建议管理员权限，热键才可靠）
cd /d D:\vocotype-cli
set ASR_ENGINE=sensevoice
.venv\Scripts\python.exe main.py

:: 回退旧 Paraformer 引擎（如需要）：
:: set ASR_ENGINE=paraformer
```

### 第 3 步：内网验证要点
- 启动日志出现 `FunASR服务器初始化，引擎=sensevoice` + `SenseVoice ONNX模型加载完成 (quantize=True)` + `已注册热键 f2` 即成功
- 模型路径解析逻辑：SenseVoice 模式下直接查 `%USERPROFILE%\.cache\modelscope\hub\models\iic\SenseVoiceSmall\`，目录存在即纯离线加载；paraformer 模式走"离线优先"的 `get_model_cache_path()`
- 若日志出现 modelscope 联网重试警告，只要最后显示"模型加载完成"就不影响；彻底规避可设环境变量 `MODELSCOPE_CACHE=%USERPROFILE%\.cache\modelscope`
- 冒烟测试（不依赖麦克风/热键）：
  ```bat
  set ASR_ENGINE=sensevoice
  .venv\Scripts\python.exe -m app.funasr_server -a 某段测试音频.wav --pretty
  ```
  输出 `"success": true` 和识别文本即链路正常（JSON 里 `"model_type": "sensevoice"` 确认用的引擎）

### 内网环境已知差异/风险
1. **麦克风权限**：Windows 设置 → 隐私 → 麦克风，需允许桌面应用
2. **管理员权限**：`keyboard` 库的全局钩子在部分企业策略下需要管理员运行
3. **杀毒/EDR 拦截**：模拟键盘输入（SendInput）可能被企业安全软件标记，需提前报备
4. **Python 版本**：本机验证用 3.13；内网若是 3.8~3.10，torch/numpy 的 wheel 要对应版本重新在外网下载

---

## 四、目录速查

```
本机关键路径
├─ 代码:      C:\Users\tianw\WorkBuddy\2026-09-01-14-57-35\vocotype-cli\
├─ venv:      C:\Users\tianw\.workbuddy\binaries\python\envs\vocotype\
├─ 模型缓存:  C:\Users\tianw\.cache\modelscope\hub\models\iic\
│              ├─ SenseVoiceSmall\            ← 默认引擎用（243MB 量化版，必需）
│              ├─ speech_paraformer-large_... ← 旧引擎回退用（228MB，可选）
│              ├─ speech_fsmn_vad_...         ← VAD（534KB，默认不加载）
│              └─ punc_ct-transformer_...     ← 标点（274MB，仅旧引擎用）
├─ 离线迁移包: C:\Users\tianw\WorkBuddy\2026-09-01-14-57-35\vocotype_offline_bundle\ (1.1GB)
├─ 测试音频:  C:\Users\tianw\WorkBuddy\2026-09-01-14-57-35\test_mix.mp3 (中英混)
│              C:\Users\tianw\WorkBuddy\2026-09-01-14-57-35\test_english.wav (纯英文)
├─ 术语词典:  vocotype-cli\terms.txt（改完 5 秒自动生效）
└─ 运行日志:  vocotype-cli\logs\log_YYYY-MM-DD.log (+ recent.wav 最近一次录音)
```
